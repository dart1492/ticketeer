import 'package:flutter/material.dart';

class CustomActiveChip extends StatelessWidget {
  const CustomActiveChip({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
